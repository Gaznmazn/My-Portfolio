const buttonNavigation = document.getElementById("navigation-btn");
const Navigation = document.body.querySelector(".head-navigation");

buttonNavigation.onclick = () => {
  Navigation.classList.toggle("view");
  if (buttonNavigation.classList.contains("view")) {
    Navigation.style;
  }
};
